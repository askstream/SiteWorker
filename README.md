# Генератор пользовательских вопросов

Агент для парсинга веб-страниц и генерации логичных вопросов на основе их контента.

## 🎯 Описание

Агент анализирует содержимое веб-страницы и генерирует 5 вопросов, которые могли бы задать пользователи после прочтения контента.

## ⚙️ Логика работы

1. Загружает HTML-контент по указанному URL
2. Извлекает текстовое содержимое из HTML
3. Отправляет текст в модель OpenAI с промптом для генерации вопросов
4. Возвращает список из 5 вопросов

## 📋 Требования

- Python 3.12+
- OpenAI API ключ

## 🚀 Установка

1. Установите зависимости:

```bash
pip install -r requirements.txt
```

2. Создайте файл `.env` в корне проекта и добавьте ваш OpenAI API ключ и модель (опционально):

```
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o
```

Если `OPENAI_MODEL` не указана, будет использована модель `gpt-4o` по умолчанию.

## 💻 Использование

### Из командной строки:

```bash
python agent.py https://example.com
```

### Программно:

```python
from agent import QuestionGeneratorAgent

agent = QuestionGeneratorAgent()
questions = agent.run("https://example.com")
print(questions)
```

## 📁 Структура проекта

```
user_questions/
├── agent.py              # Основной модуль агента
├── openai_module.py      # Модуль для работы с OpenAI API
├── requirements.txt      # Зависимости проекта
└── README.md             # Документация
```

## 🔧 Настройки

### Выбор модели

Модель можно задать тремя способами (в порядке приоритета):

1. **Через .env файл** (рекомендуется):
   ```
   OPENAI_MODEL=gpt-3.5-turbo
   ```

2. **Программно при создании агента**:
   ```python
   agent = QuestionGeneratorAgent(model="gpt-3.5-turbo")
   ```

3. **По умолчанию**: используется `gpt-4o`, если модель не указана ни одним из способов выше.

### Количество вопросов

По умолчанию генерируется 5 вопросов. Можно изменить:

```python
questions = agent.run("https://example.com", num_questions=10)
```

## 🛡️ Обработка ошибок

Агент автоматически повторяет запросы при сбоях:
- До 3 попыток для загрузки страницы
- До 3 попыток для запросов к OpenAI API
- Экспоненциальная задержка между попытками

## 📝 Пример вывода

```
============================================================
Сгенерированные вопросы:
============================================================
1. Какова основная цель этого продукта?
2. Какие преимущества он предоставляет?
3. Как начать использовать этот сервис?
4. Сколько стоит подписка?
5. Есть ли бесплатная пробная версия?
============================================================
```

## ⚠️ Примечания

- Убедитесь, что у вас есть активный OpenAI API ключ с достаточным балансом
- Некоторые сайты могут блокировать автоматические запросы
- Для больших страниц текст может быть обрезан из-за ограничений модели

