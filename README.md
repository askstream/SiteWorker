# Агент для анализа веб-страниц

Агент для парсинга веб-страниц и выполнения различных задач анализа контента.

## 🎯 Описание

Агент анализирует содержимое веб-страницы и выполняет различные задачи:
- **Генерация пользовательских вопросов** - создает логичные вопросы, которые могли бы задать пользователи
- **Классификация типа контента** - определяет тип сайта (лендинг, блог, маркетплейс и т.д.)
- **UX-рецензент** - анализирует сайт и генерирует UX-отчёт с рекомендациями по улучшению

## ⚙️ Доступные задачи

### 1. Генератор пользовательских вопросов

Логика работы:
1. Загружает HTML-контент по указанному URL
2. Извлекает текстовое содержимое из HTML
3. Отправляет текст в модель OpenAI с промптом для генерации вопросов
4. Возвращает список из 5 вопросов

### 2. Классификатор типа контента

Логика работы:
1. Получает HTML, извлекает текст
2. Отправляет текст модели с системной ролью для классификации
3. Возвращает тип сайта + краткое объяснение

### 3. UX-рецензент сайта

Логика работы:
1. Загружает HTML по ссылке
2. Извлекает текст и формирует промпт UX-эксперта
3. Отправляет текст в модель с системной ролью
4. Получает отчёт: достоинства, слабые места и 5 предложений по улучшению UX

## 📋 Требования

- Python 3.12+
- OpenAI API ключ

## 🚀 Установка

1. Установите зависимости:

```bash
pip install -r requirements.txt
```

2. Создайте файл `.env` в корне проекта и добавьте ваш OpenAI API ключ и модель (опционально):

```
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o
```

Если `OPENAI_MODEL` не указана, будет использована модель `gpt-4o` по умолчанию.

## 💻 Использование

### Из командной строки:

```bash
# Генерация вопросов (по умолчанию)
python agent.py https://example.com
python agent.py https://example.com questions

# Классификация типа контента
python agent.py https://example.com classify

# UX-анализ и рекомендации
python agent.py https://example.com ux

# Выполнить все задачи
python agent.py https://example.com all
```

### Программно:

#### Генерация вопросов:

```python
from agent import QuestionGeneratorAgent

agent = QuestionGeneratorAgent()
questions = agent.run("https://example.com")
print(questions)
```

#### Классификация типа контента:

```python
from agent import ContentClassifierAgent

agent = ContentClassifierAgent()
result = agent.run("https://example.com")
print(f"Тип: {result['type']}")
print(f"Объяснение: {result['explanation']}")
```

#### UX-анализ:

```python
from agent import UXReviewerAgent

agent = UXReviewerAgent()
result = agent.run("https://example.com")
print("Достоинства:", result['strengths'])
print("Слабые места:", result['weaknesses'])
print("Рекомендации:", result['recommendations'])
```

#### Выполнение всех задач:

```python
from agent import SiteAgent

agent = SiteAgent()
results = agent.run_all("https://example.com")
print(results['questions'])
print(results['content_type'])
print(results['ux_report'])
```

## 📁 Структура проекта

```
.
├── agent.py              # Основной модуль агента с несколькими задачами
├── openai_module.py      # Модуль для работы с OpenAI API
├── requirements.txt      # Зависимости проекта
└── README.md             # Документация
```

## 🔧 Настройки

### Выбор модели

Модель можно задать тремя способами (в порядке приоритета):

1. **Через .env файл** (рекомендуется):
   ```
   OPENAI_MODEL=gpt-3.5-turbo
   ```

2. **Программно при создании агента**:
   ```python
   agent = QuestionGeneratorAgent(model="gpt-3.5-turbo")
   ```

3. **По умолчанию**: используется `gpt-4o`, если модель не указана ни одним из способов выше.

### Количество вопросов

По умолчанию генерируется 5 вопросов. Можно изменить:

```python
questions = agent.run("https://example.com", num_questions=10)
```

## 🛡️ Обработка ошибок

Агент автоматически повторяет запросы при сбоях:
- До 3 попыток для загрузки страницы
- До 3 попыток для запросов к OpenAI API
- Экспоненциальная задержка между попытками

## 📝 Примеры вывода

### Генерация вопросов:

```
============================================================
Сгенерированные вопросы:
============================================================
1. Какова основная цель этого продукта?
2. Какие преимущества он предоставляет?
3. Как начать использовать этот сервис?
4. Сколько стоит подписка?
5. Есть ли бесплатная пробная версия?
============================================================
```

### Классификация типа контента:

```
============================================================
Классификация типа контента:
============================================================
Тип: Лендинг

Объяснение: Страница содержит призыв к действию, описание продукта,
преимущества и форму обратной связи, что характерно для лендинга.
============================================================
```

### UX-анализ:

```
============================================================
UX-ОТЧЁТ
============================================================

✅ Достоинства:
  1. Понятная навигация
  2. Четкая структура контента
  3. Хорошая читаемость текста

⚠️  Слабые места:
  1. Отсутствие призыва к действию
  2. Слишком длинные формы
  3. Недостаточно визуальных элементов

💡 Рекомендации по улучшению UX:
  1. Добавить четкий призыв к действию на главной странице
  2. Упростить формы регистрации
  3. Улучшить мобильную версию сайта
  4. Добавить больше визуального контента
  5. Оптимизировать время загрузки страницы
============================================================
```

### Все задачи:

```
============================================================
РЕЗУЛЬТАТЫ АНАЛИЗА СТРАНИЦЫ
============================================================

📋 Классификация типа контента:
Тип: Лендинг
Объяснение: Страница содержит призыв к действию...

❓ Сгенерированные вопросы:
1. Какова основная цель этого продукта?
2. Какие преимущества он предоставляет?
...

🎨 UX-ОТЧЁТ:
  ✅ Достоинства:
    1. Понятная навигация
    2. Четкая структура контента
  ...
  ⚠️  Слабые места:
    1. Отсутствие призыва к действию
  ...
  💡 Рекомендации по улучшению UX:
    1. Добавить четкий призыв к действию
  ...
============================================================
```

## ⚠️ Примечания

- Убедитесь, что у вас есть активный OpenAI API ключ с достаточным балансом
- Некоторые сайты могут блокировать автоматические запросы
- Для больших страниц текст может быть обрезан из-за ограничений модели

